{
	"name": "my-very-first-package",
	"version": "0.0.1",
	"description": "Well this is how we create package.json",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon app.js",
		"start": "node app.js"
	},
	"keywords": [
		"node",
		"npm",
		"fun"
	],
	"author": "Flow",
	"license": "MIT",
	"dependencies": {
		"cat-names": "^3.1.0"
	}
}
